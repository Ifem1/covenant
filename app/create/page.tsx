'use client'; import Link from 'next/link'; import {useState} from 'react';
export default function Create(){const [saved,setSaved]=useState(false);return <main className="page"><header className="top"><Link className="brand" href="/">COVENANT / AUTHOR</Link><Link className="nav" href="/">CLOSE ×</Link></header><section className="section" style={{maxWidth:760}}><div className="eyebrow">NEW SEALED DOCUMENT / DRAFT</div><h1 className="narrow" style={{fontSize:64,margin:'16px 0'}}>AUTHOR YOUR PUBLIC PROMISES.</h1><p style={{color:'#b9c1c2'}}>Set the words, evidence surfaces, schedule, and consequence. Once funded and activated, the definition is frozen.</p><form onSubmit={e=>{e.preventDefault();setSaved(true)}}><label className="mono">SERVICE NAME<input required placeholder="Atlas Relay" /></label><label className="mono">PUBLIC DESCRIPTION<textarea required placeholder="API and agent infrastructure for teams that need reliable routing." /></label><label className="mono">RECOVERY RECIPIENT<input required placeholder="0x…" /></label><label className="mono">MINIMUM BOND (GEN)<input required type="number" min="1" placeholder="12500" /></label><label className="mono">PUBLIC SOURCES (2–5)<textarea required placeholder="https://status.example.com\nhttps://example.com/incidents" /></label><button className="cta" type="submit">{saved?'DRAFT READY — CONTINUE →':'SEAL DRAFT →'}</button></form></section></main>}
