# Security

Sources are HTTPS-only, bounded, credential-free, and limited to 2–5. No server signer or backend is used. Slash amounts are sealed bps, never model-selected. A failed observation is not proof of absence; duplicate audit IDs and cumulative caps prevent replay.
