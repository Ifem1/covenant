import Link from 'next/link'; export default async function Receipt({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main className="page"><header className="top"><Link className="brand" href="/">COVENANT / RECEIPT</Link><span className="mono">AUDIT {id}</span></header><section className="section"><div className="eyebrow">FINALIZED CONSENSUS RECEIPT</div><h1 className="narrow" style={{fontSize:80}}>INCONCLUSIVE.</h1><p style={{fontSize:20,color:'#b9c1c2'}}>The contract state is unchanged. Evidence was insufficient to confirm breach, so no GEN moved.</p><div className="card"><div className="mono">OUTCOME / INCONCLUSIVE<br/>SLASH / 0 BPS<br/>STATE / GOOD_STANDING<br/>TRANSACTION / 0x…</div></div><Link className="cta" href="/verify">VERIFY AUTHORITATIVE STATE →</Link></section></main>}
