'use client'; import {useState} from 'react'; export default function Verify(){const [id,setId]=useState('');return <main className="page"><header className="top"><a className="brand" href="/">COVENANT</a><span className="mono">READ-ONLY VERIFIER</span></header><section className="section" style={{maxWidth:700}}><div className="eyebrow">NO TRUST REQUIRED</div><h1 className="narrow" style={{fontSize:80}}>READ THE STATE.</h1><p style={{color:'#b9c1c2'}}>Query the Registry and Vault directly. This verifier never relies on a server-side copy.</p><input value={id} onChange={e=>setId(e.target.value)} placeholder="Covenant ID or address"/><div className="card" style={{marginTop:16}}><span className="status">● {id?'LOOKUP READY':'AWAITING IDENTIFIER'}</span><p className="mono">CHAIN ID · 61999<br/>REGISTRY · not configured<br/>VAULT · not configured</p></div></section></main>}
