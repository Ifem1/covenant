# Verification

Run `npm run typecheck`, `npm run test`, `npm run build`, and `python -m compileall contracts`. Live Studionet tests are opt-in because they require a user-funded wallet. The network test rejects chain 61997 and any RPC drift.
