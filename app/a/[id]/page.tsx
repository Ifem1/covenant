'use client'; // @ts-nocheck
import Link from 'next/link'; import {useEffect,useState} from 'react'; import {readAudit} from '../../../lib/contract/adapters';
export default function Receipt({params}:{params:{id:string}}){const id=params.id;const [audit,setAudit]=useState<any>();const [error,setError]=useState('');useEffect(()=>{if(!id){setError('Missing audit ID');return}readAudit(id).then(setAudit).catch(e=>setError(e instanceof Error?e.message:'Audit not found'))},[id]);return <main className="page"><header className="top"><Link className="brand" href="/">COVENANT / RECEIPT</Link><span className="mono">AUDIT {id}</span></header><section className="section">{error?<><h1>READ ERROR</h1><p>{error}</p></>:!audit?<p className="mono">READING FINALIZED AUDIT…</p>:<><div className="eyebrow">FINALIZED CONSENSUS RECEIPT</div><h1 className="narrow" style={{fontSize:80}}>{audit.outcome}</h1><div className="card"><div className="mono">OUTCOME / {audit.outcome}<br/>INTERVAL / {String(audit.interval_start)} — {String(audit.interval_end)}<br/>SLASH / {String(audit.slash_bps)} BPS<br/>SETTLED / {String(audit.settled)}<br/>DEFINITION / {audit.definition_hash}</div></div>{(audit.findings||[]).map((f:any)=><article className="card" key={String(f.clause_id)}><h3>CLAUSE {String(f.clause_id)} · {f.finding}</h3><p>{f.reason}</p>{(f.evidence||[]).map((e:any)=><p className="mono" key={String(e.source_id)}>SOURCE {String(e.source_id)} · {e.excerpt}</p>)}</article>)}</>}</section></main>}
