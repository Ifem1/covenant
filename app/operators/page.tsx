'use client';
import Link from 'next/link'; import {useEffect,useState} from 'react'; import {readCanonicalVault} from '../../lib/contract/adapters';
export default function Operators(){const [state,setState]=useState('Loading Registry…');useEffect(()=>{readCanonicalVault().then(v=>setState(`Canonical Vault: ${String(v)}`)).catch(e=>setState(e instanceof Error?e.message:'Registry unavailable'))},[]);return <main className="page"><header className="top"><Link className="brand" href="/">COVENANT</Link><span className="mono">PUBLIC REGISTRY</span></header><section className="section"><div className="eyebrow">LIVE CONTRACT STATE</div><h1 className="narrow" style={{fontSize:76}}>OPERATORS ON RECORD.</h1><div className="card"><span className="status">● REGISTRY READ</span><p className="mono">{state}</p><p>Live covenant enumeration will be shown when the Registry enumeration view is configured.</p><Link className="cta" href="/create">CREATE COVENANT →</Link></div></section></main>}
