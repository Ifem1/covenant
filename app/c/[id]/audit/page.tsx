import Link from 'next/link'; export default async function Audit({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main className="page"><header className="top"><Link className="brand" href={`/c/${id}`}>← COVENANT {id}</Link><span className="status">AUDIT CHAMBER</span></header><section className="section"><div className="eyebrow">PERMISSIONLESS INSPECTION / NO CALLER REWARD</div><h1 className="narrow" style={{fontSize:76}}>AUDIT THE INTERVAL.</h1><div className="grid"><div className="card"><span className="tag">INTERVAL</span><h3>01 SEP — 05 SEP 2026</h3><p>Bound to definition hash 0x9c…44a. Three clauses, three sources.</p></div><div className="card"><span className="tag">SOURCE SURFACES</span><h3>2 / 3 AVAILABLE</h3><p>Status page available. Incident archive available. Changelog unavailable.</p></div><div className="card"><span className="tag">SAFE OUTCOME</span><h3 style={{color:'var(--amber)'}}>INCONCLUSIVE</h3><p>No slash. Retry when evidence coverage is sufficient.</p></div></div><button className="cta">CONNECT WALLET & RUN AUDIT →</button></section></main>}
