import {describe,it,expect} from 'vitest'; import {Source,Finding} from '../lib/validation';
describe('evidence safety',()=>{it('rejects private sources',()=>expect(()=>Source.parse('http://localhost:3000/a')).toThrow());it('rejects non-HTTPS sources',()=>expect(()=>Source.parse('http://example.com/a')).toThrow());it('allows abstention',()=>expect(Finding.parse({clauseId:1,finding:'INCONCLUSIVE',severity:'NONE',evidence:[],observedEventTimestamp:0,reason:'evidence unavailable'}).finding).toBe('INCONCLUSIVE'));it('rejects evidence on unresolved findings',()=>expect(()=>Finding.parse({clauseId:1,finding:'UNAVAILABLE',severity:'NONE',evidence:[{sourceId:1,excerpt:'x'}],observedEventTimestamp:0,reason:'unavailable'})).toThrow());it('rejects nonzero timestamp on compliant findings',()=>expect(()=>Finding.parse({clauseId:1,finding:'COMPLIED',severity:'NONE',evidence:[],observedEventTimestamp:42,reason:'ok'})).toThrow());it('bounds evidence references',()=>expect(()=>Finding.parse({clauseId:1,finding:'BREACHED',severity:'HIGH',evidence:[{sourceId:1,excerpt:'x'.repeat(301)}],observedEventTimestamp:1,reason:'breach'})).toThrow())});
