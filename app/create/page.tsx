'use client';
import Link from 'next/link'; import {useState} from 'react'; import {createCovenant} from '../../lib/contract/adapters'; import type {Address} from 'viem';
export default function Create(){const [phase,setPhase]=useState('');const [error,setError]=useState('');const [hash,setHash]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError('');setHash('');const f=new FormData(e.currentTarget);const eth=(window as Window & {ethereum?:unknown}).ethereum;if(!eth){setError('Connect a wallet before submitting');return}try{const sources=String(f.get('sources')||'').split('\n').map((url,i)=>({source_id:i+1,url:url.trim()})).filter(x=>x.url);const tx=await createCovenant(eth as never,{service:String(f.get('service')),description:String(f.get('description')),recovery:String(f.get('recovery')) as Address,minimumBond:BigInt(String(f.get('bond')))*1000000000000000000n,interval:BigInt(String(f.get('interval'))),term:BigInt(String(f.get('term'))),clauses:[{clause_id:1,text:String(f.get('clause')),slash_bps:Number(f.get('slash')),minimum_sources:Math.min(2,sources.length)}],sources},setPhase);setHash(tx)}catch(e){setError(e instanceof Error?e.message:'Transaction failed')}}return <main className="page"><header className="top"><Link className="brand" href="/">COVENANT / AUTHOR</Link><Link className="nav" href="/">CLOSE ×</Link></header><section className="section" style={{maxWidth:760}}><div className="eyebrow">LIVE REGISTRY TRANSACTION / DRAFT</div><h1 className="narrow" style={{fontSize:64,margin:'16px 0'}}>AUTHOR A COVENANT.</h1><p style={{color:'#b9c1c2'}}>This form submits directly to the configured Registry on Studionet.</p><form onSubmit={submit}><label className="mono">SERVICE NAME<input name="service" required /></label><label className="mono">DESCRIPTION<textarea name="description" required /></label><label className="mono">RECOVERY ADDRESS<input name="recovery" required /></label><label className="mono">MINIMUM BOND (GEN)<input name="bond" required type="number" min="1" /></label><label className="mono">INTERVAL (SECONDS)<input name="interval" required type="number" min="1" /></label><label className="mono">TERM (SECONDS)<input name="term" required type="number" min="1" /></label><label className="mono">CLAUSE TEXT<input name="clause" required /></label><label className="mono">SLASH (BPS)<input name="slash" required type="number" min="0" max="10000" /></label><label className="mono">HTTPS SOURCES, ONE PER LINE<textarea name="sources" required /></label><button className="cta" type="submit" disabled={phase==='AWAITING_SIGNATURE'}>{phase||'SUBMIT CREATE TRANSACTION'}</button></form>{hash&&<p className="mono">FINALIZED TRANSACTION · {hash}</p>}{error&&<p role="alert" style={{color:'#e86d62'}}>{error}</p>}</section></main>}
