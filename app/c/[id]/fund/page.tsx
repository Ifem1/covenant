import Link from 'next/link'; import Client from './Client'; export default async function Fund({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main className="page"><header className="top"><Link className="brand" href={`/c/${id}`}>← DOCUMENT</Link><span className="mono">FUND / {id}</span></header><section className="section" style={{maxWidth:700}}><div className="eyebrow">BOND CUSTODY / USER SIGNATURE REQUIRED</div><h1 className="narrow" style={{fontSize:72}}>CLOSE THE LATCH.</h1><div className="card"><h3>Minimum bond</h3><h1 className="narrow" style={{fontSize:64}}>12,500 GEN</h1><p>Your wallet signs a payable transaction directly. The recovery recipient is sealed in the Registry; the caller cannot choose it.</p><Client id={id}/></div></section></main>}
